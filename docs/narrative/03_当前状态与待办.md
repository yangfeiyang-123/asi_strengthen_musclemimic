# 当前状态与待办

> 更新：2026-09-20。主线以 `进展/工作汇报0816.pdf` 为准：**S1 轨迹跟踪 + 肌电奖励 → S2 隐空间蒸馏 → S3 冻结 decoder 的 LAB 自适应击球**。
> 旧的 1256 行路线图已归档到 `docs/archive/03_仓库改进与待办路线图_20260812.md`；本文只保留仍然有效的判断与待办。
> 运行进度唯一记录：`experiments/EXPERIMENT_LOG.md`。

## 1. 三阶段就绪度

| 阶段 | 代码/合同 | 正式实验 | 说明 |
|---|---|---|---|
| S0 数据与肌电 | 完成 | 完成（有保留） | aug100 80/20 release 与 verified tube 已落盘。保留：正手高远球未启用下肢接触优化；肌电为单 subject 单 session 11 trials，impact 独立证据 0，与视频受试者非配对 |
| S1 跟踪 + 肌电奖励 | 完成，有测试 | **12/15 到 800M endpoint，未晋级** | endpoint 在 `checkpoints/stage1/`（seed0 T0–T4、seed1 T1–T4、seed2 T2–T4）；缺 T0 s1（本机 09-11 中断于 300M）、T0 s2、T1 s2；pairwise gate / post-hoc physiology / blind review 未跑 |
| 球拍质量课程 | 完成 | 未开始（**可起训**，2026-09-20 已 smoke） | 6 个组全部未开始；依赖 T3 晋级。2026-09-20 球拍/握拍/羽毛球资产与 legacy 22/5 数据已核对到位（`docs/runbooks/racket_grip_hit_assets_20260920.md`）；`derived_rigid` 球拍参考不需要 event bank；入口 `stage2_racket_v2/conf_fullbody_forehand_clear_aug100_racket_derived_rigid.yaml`，从 T3 seed0 起训的 2M 步 smoke 已跑通（步骤与踩坑见 runbook §持拍训练） |
| S2 蒸馏 | 完成，有测试 | **未开始** | S2-A direct 与 S2-B/C/D/E latent family 都没有 run；PPT p19 的表没有仓库来源 |
| S3 LAB 击球 | 完成，可运行 | **未开始** | 只有 v1/impact_recovery_v2 两个 LAB spec；v3–v45 direct-residual 线已归档 |
| 双人对打 | 完成（CPU） | 不排期 | 保留为 S3 之后的评估场；缺 MJX 版与策略桥 |

## 2. 立即待办（按顺序）

1. **补齐 Stage 1 剩余 3 个 run**：T0 s1 本机 2026-09-11 中断于 300M（进程消失、无 traceback），决定 auto_resume 或 fresh 重跑；T0 s2、T1 s2 未开始。（80/20 版 t2/t3/t4 配置已于 2026-09-20 照 t1 派生补齐，registry 的 forehand_clear 正式 family 已指向 aug100。）
2. **在 12 个 endpoint 上跑证据链**：post-hoc physiology、T3-vs-T4 / T3-vs-T0 pairwise gate、evidence index（缺 3 个 leaf 前只能出 partial）、T3 seed0 blind review；输入为 `checkpoints/stage1/`，合同见 `docs/runbooks/peasd_implementation_guide.md` §3。
3. **修订 PPT 0816**：p18 的 S2 表按代码命名重排（S2-A direct、S2-B 无 EMG、S2-C 主方法、S2-D shuffled、S2-E 无 dropout）；p17 的 T2 标签改为"仅协同约束"；p16/17 注明"40/10 子集、单 seed、无 T0"；p19 标为预期值或删除。
4. **回传 Stage 3 资产**：`.gitignore` 全局 `*.yaml` 挡住的 v31–v45 spec 用 `git add -f` 入库。v24c checkpoint 与冻结 base（ckpt156）已于 2026-09-20 随 HF 迁移包落到 `artifacts/stage3_demo_sources/`；feed bank 与 CEM 产物源服务器也已不存在（见 `docs/runbooks/racket_grip_hit_assets_20260920.md`），S3 direct-residual 演示不再追求原样复现。
5. **跑 T3 + anchor v2 单 seed**（`peasd_lite_v1/conf_fullbody_forehand_clear_aug100_peasd_t3_anchor_v2.yaml`）：12 个 endpoint 的诊断显示原 tube 让 T2/T3 把 TRIlat、PT 等被测肌肉关到 0、负荷转到未测的 TRIlong/PQ；v2 见 `docs/narrative/02` §8.5。与 T3 seed0 对比 TRIlat/PT 是否恢复、协同指标是否保持。
6. **T3 晋级后**：球拍课程 25→100% → 一次 shared teacher collection → S2-A → S2-B 锁 architecture → S2-C/D/E → S3 H1/H2/H3。顺序不可跳步。

## 需要重跑的实验（2026-09-18 anchor v2 之后）

anchor v2 改变了 T1、T3、T4 三个含激活项的 arm 的奖励定义；T0（无肌电）和 T2（仅协同）不含 anchor 项，**不需要重跑**，已完成的 T0 s0 和 T2 s0/s1/s2 endpoint 继续有效。

| 优先级 | run | 配置（`fullbody/` 下 `--config-name=`） | seed | 预算 | 目的 |
|---|---|---|---|---|---|
| P0 | T3 + anchor v2 | `config_specific_task/stage1_body/peasd_lite_v1/conf_fullbody_forehand_clear_aug100_peasd_t3_anchor_v2` | 0 | 800,010,240 | 验证修正是否把 TRIlat / PT 救回来、协同指标是否保持；与旧 T3 s0（`checkpoints/stage1/seed0/T3`）对比 |
| P0 | T0 seed 1 | `…/conf_fullbody_forehand_clear_aug100_peasd_t0` + `experiment.seeds=[1]` | 1 | 800,010,240 | 本机 09-11 中断在 300M，续训或重跑（T0 定义未变） |
| P1 | T4 + anchor v2 | `…/conf_fullbody_forehand_clear_aug100_peasd_t4_anchor_v2` | 0 | 800,010,240 | v2 下的负对照；只有 T3v2 优于 T4v2 才能说 v2 的改善来自真实相位 |
| P1 | T1 + anchor v2 | `…/conf_fullbody_forehand_clear_aug100_peasd_t1_anchor_v2` | 0 | 800,010,240 | 检验单独激活项在 v2 下是否从"几乎无效"变为有效 |
| P2 | T3v2 / T4v2 / T1v2 | 同上 + `experiment.seeds=[1]` / `[2]` 与对应 run_id | 1, 2 | 800,010,240 | 补成 3 seed matched family |
| P2 | T0 seed 2、T2 无 | `…_peasd_t0` + `experiment.seeds=[2]` | 2 | 800,010,240 | 补齐 T0 三个 seed；T2 已齐 |

顺序建议：先跑 P0 的 T3v2 s0。它若不能恢复 TRIlat / PT，就回到 preset 调 `anchor_inside_weight` / `anchor_burst_weight`，不必往下铺 P1、P2。
旧的 T1 s0/s1、T3 s0/s1/s2、T4 s0/s1/s2 endpoint 保留为 anchor v1 的结果，论文里可作为"v1 tube 漏洞"的证据，不再是主线。

启动方式不变：仓库根目录 `scripts/run_fullbody_training.sh --config-name=<上表> wandb.mode=online`，一张物理 GPU 一个 run，先 `--cfg job --resolve` 核对
`emg_consistency` 块含 `anchor_inside_weight: 0.5`、`anchor_scale_floor: 0.05`、`total_timesteps: 800010240`。目标机必须有 `artifacts/emg_human_review_v2/verified_tubes/forehand_high_clear/`
（gitignored，按 `docs/runbooks/server_deployment.md` 迁移），否则 reward 会 fail-closed。换 seed 时同时改 `experiment.run_id` 与 `wandb.name`（把 `_s0` 换成 `_s1`），每个 seed 独立 run 目录。

## 3. 已经决定、不再讨论的事

- 肌电奖励挂在 body-only Stage 1 matched family，球拍课程不重复加肌电奖励（`02` 文档第 4 节）。
- canonical decoder 是 direct MLP；fixed synergy / W+R 是归档消融线。
- Stage 3 主方法是 LAB；direct-residual 线只保留 reachability gate 与 aero 校准结论。
- 消融命名以代码为准（`02` 文档第 24 节），PPT 向代码对齐。
- 数据组唯一为 aug100 80/20；40/10 与 legacy 22/5 结果不进入 formal family。

## 4. 已知工程债

- verified tube 与 mapping 位于 gitignored `artifacts/`，clean clone 无法启动肌电奖励；迁移必须按 `docs/runbooks/server_deployment.md` 走资产清单。
- `tests/unit` 中 44 个失败几乎全是私有资产缺失、`/data3` 旧路径与被 ignore 的 spec；唯一像代码问题的是
  `tests/unit/test_ppo.py::test_evaluate_all_rollout_uses_transition_done_after_autoreset`（`apply()` 缺 `mutable`）。
- 计划文档写 128-feed held-out，现行 spec 为 64；定稿 v46 时统一。
- 仓库内 `configs/physiology/emg_badminton_synergy_16_v2_myofullbody_observation_v1.json` 仍为 provisional，训练实际读取 verified 副本；对外描述时写"16 采集 / 15 可比"。

## 5. 归档清单

`docs/archive/` 中的文件与归档原因见该目录 `README.md`。归档不等于删除代码：ChinaJump、fixed synergy、Graph-NMF、肌束连续性、握拍编辑器的代码与测试仍在仓库中，只是不再出现在主线文档里。
