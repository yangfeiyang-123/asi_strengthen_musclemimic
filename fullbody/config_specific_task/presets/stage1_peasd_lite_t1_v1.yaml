# @package _global_
experiment:
  stage1_peasd:
    arm: T1
    control_kind: activation_anchor_only
  env_params:
    reward_params:
      emg_consistency:
        enabled: true
        arm: T1
        anchor_weight_max: 0.02
        synergy_weight_max: 0.0
        synergy_phase_shuffle_offset_bins: 0
